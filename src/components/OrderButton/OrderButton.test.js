import React from 'react';
import { shallow, mount } from 'enzyme';

import OrderButton from './OrderButton';

describe('<OrderButton></OrderButton>', () => {
  xit('Should component render children', () => {
    
  });
});

